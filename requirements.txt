fastapi
uvicorn[standard]
litellm
python-dotenv
pydantic
requests
sentence-transformers
scikit-learn
bcrypt
cryptography

# Turso (hosted SQLite) support -- only imported when TURSO_DATABASE_URL is set.
libsql; platform_machine == "x86_64" or platform_system == "Darwin"
